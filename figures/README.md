# Figures — the results story

Rendered by `python -m scripts.make_figures` from the committed measurement JSON in [`../results/`](../results/) (regenerate that data with `scripts/run_measurements.sh` + `scripts/measure_pool.py`). Each `.png` is for the write-up; the matching `.html` is interactive.

**Start with [`exec_summary.png`](exec_summary.png)** — every headline edge with its 95% bootstrap CI (the honest "are the edges real?" view). Narrative: [../THESIS.md](../THESIS.md). Bibliography: [../references.md](../references.md).

| Figure | Section | What it shows |
|---|---|---|
| [`exec_summary.png`](exec_summary.png) | §summary | EXECUTIVE SUMMARY — every headline edge as a point with its 95% bootstrap CI. Gray = the CI straddles 0 (effect is within per-seed noise); green/red = CI excludes 0. 1/4 edges are statistically distinguishable from zero at these sample sizes — the honest takeaway: the agent is directionally positive but the edges are marginal, exactly what rigorous variance accounting should show (see references.md §2; THESIS.md). |
| [`headline.png`](headline.png) | §8 | Headline (§8): the fixed-vs-myopic RL agent learns to beat the myopic EV baseline. Left axis = held-out win rate; right axis = mean chip diff with a ±1 SEM ribbon over training. Final 32/50 matches, +560 mean chips, paired p=0.0466, 95% CI [+0, +1040]. The CI's lower bound near 0 shows the edge is real but marginal — variance accounting matters (see exec_summary). |
| [`pool_leaderboard.png`](pool_leaderboard.png) | §10 | §10 generalist (RL = belief + sharp HMM + PnL feed + opponent-mix, multi-hand chips, 12000 steps): cross-agent leaderboard over 16 held-out seeds × 100 hands. RL tops the leaderboard and beats each of the {myopic, tilt, random} adaptive opponents head-to-head (13-3 / 9-7 / 12-4); it loses H2H to the analytic Kelly (5-11). The §10 win is the leaderboard + adaptive-pool result, not Kelly H2H. RL mean +209, 95% CI [-31, +450] — this CI includes 0, so the lead is NOT significant at 16 seeds (more seeds / variance reduction needed to confirm). |
| [`pool_sweep.png`](pool_sweep.png) | §10 | §10: the same RL generalist dropped into a round-robin of static (tight × aggression) personalities. RL ranks #6/17 (RL +312 vs the best static cell +434 at t0.20/a0.50) — it beats the adaptive pool (left figure) and does NOT top it (the round-robin rewards farming the weakest static cells, not RL's vs-adaptive-pool objective). Blue = net winner. |
| [`pool_pnl_box.png`](pool_pnl_box.png) | §10 | §10: per-seed net-chip distribution per agent (the spread behind the leaderboard means — poker variance is wide). |
| [`icm_edge_bubble.png`](icm_edge_bubble.png) | §13 | §13 ICM edge (3-player, ladder 65/35/0): per-init-seed ICM-reward minus chip-reward mean tournament prize over 6 reproducible seeds — ICM-reward UNDERPERFORMING the chip reward here (mean -122, 2/6 seeds ICM>chips). The earlier §13 small-positive estimate came from 3 noisier unseeded inits; so ICM UNDERPERFORMS the chip reward on average at this scale. |
| [`icm_edge_mild.png`](icm_edge_mild.png) | §13 | §13 ICM edge (3-player, ladder 50/30/20): per-init-seed ICM-reward minus chip-reward mean tournament prize over 6 reproducible seeds — ICM-reward UNDERPERFORMING the chip reward here (mean -146, 1/6 seeds ICM>chips). The earlier §13 small-positive estimate came from 3 noisier unseeded inits; so ICM UNDERPERFORMS the chip reward on average at this scale. |
| [`blockB_action_grid.png`](blockB_action_grid.png) | §16 | B2 (§16): the finer 7-action grid does NOT beat the default 5-action grid (both strongly beat myopic) — extra sizings just spread the budget over more actions. Mean held-out chip diff vs myopic, per init seed. 5-grid stays the default. |
| [`blockB_bust_clip.png`](blockB_bust_clip.png) | §15 | B3 (§15): widening the multi-hand bust clip does NOT help — the tight 3.0 ('old') clip has the best mean held-out chip diff vs myopic (averaged over 6 init seeds); '4.6' and 'wide' (≈6.9) regress and destabilise some inits (high weight-init variance). |
| [`blockB_bust_rate.png`](blockB_bust_rate.png) | §15 | B3 (§15): bust rate per clip (avg over seeds). The tight 3.0 clip also has the lowest bust rate — in this heads-up winner-take-all format bust ≈ 1 − win rate, so there is no independent risk lever to recover by un-clipping the ruin signal. |
| [`blockB_selfplay.png`](blockB_selfplay.png) | §18 | B4 (§18): snapshot self-play vs the fixed-vs-myopic recipe, mean RL net chips vs each opponent (averaged over init seeds). Self-play trades the myopic bench for more tilt-robustness but is higher-variance — fixed stays the dependable recipe. |
| [`blockB_tilt_decouple.png`](blockB_tilt_decouple.png) | §17 | B5 (§17): tilt-bonus configurations, mean RL net chips vs each opponent (avg over seeds). The PnL feature ALONE (pnl_nobonus, +249) is best; adding the naive bonus drags it down (pnl_naive +46 — the footgun, worse than the feature alone); the decouple (pnl_decouple +138) is safe but no net gain; the bonus WITHOUT the PnL feature (nopnl_bonus −159) collapses. Feature and bonus are substitutes, not complements. |
| [`rollout_fe.png`](rollout_fe.png) | §14 | B1 (§14): warmed-belief rollout fold-equity, net chips of the rollout vs each opponent (60 paired seeds × 200 hands). Warming LARGELY fixes the cold-FE over-bluff disaster vs myopic/tilt (cold_fe → warm_fe rows: myopic −1133→−67, tilt flips positive), but it is NOT a uniform win — it is worse vs random and the nit punishes it — so fold-equity stays OFF by default. Blue = rollout wins chips. |
