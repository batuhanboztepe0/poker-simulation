# Figures — the results story

Rendered by `python -m scripts.make_figures` from the committed measurement JSON in [`../results/`](../results/) (regenerate that data with `scripts/run_measurements.sh` + `scripts/measure_pool.py`). Each `.png` is for the README; the matching `.html` is interactive.

| Figure | Section | What it shows |
|---|---|---|
| [`headline.png`](headline.png) | §8 | Headline (§8): the fixed-vs-myopic RL agent learns to beat the myopic EV baseline. Left axis = held-out win rate; right axis = mean chip diff with a ±1 SEM ribbon over training. Final 32/50 matches, +560 mean chips, paired p=0.0466. |
| [`pool_leaderboard.png`](pool_leaderboard.png) | §10 | §10 generalist (RL = belief + opponent-mix + PnL feed, 12000 steps): cross-agent leaderboard over 16 held-out seeds × 100 hands. RL tops the leaderboard vs the {myopic, tilt, random} pool + Kelly — it generalises and beats the whole adaptive pool (the §10 win). |
| [`pool_sweep.png`](pool_sweep.png) | §10 | §10: the same RL generalist dropped into a round-robin of static (tight × aggression) personalities. RL ranks #6/17 (RL +312 vs the best static cell +434 at t0.20/a0.50) — it beats the adaptive pool (left figure) and does NOT top it (the round-robin rewards farming the weakest static cells, not RL's vs-adaptive-pool objective). Blue = net winner. |
| [`pool_pnl_box.png`](pool_pnl_box.png) | §10 | §10: per-seed net-chip distribution per agent (the spread behind the leaderboard means — poker variance is wide). |
| [`icm_edge_bubble.png`](icm_edge_bubble.png) | §13 | §13 ICM edge (3-player, ladder 65/35/0): per-init-seed ICM-reward minus chip-reward mean tournament prize over 6 reproducible seeds — ICM-reward UNDERPERFORMING the chip reward here (mean -122, 2/6 seeds ICM>chips). The earlier §13 small-positive estimate came from 3 noisier unseeded inits; at this scale per-seed variance (±300+) dominates, so there is no robust concavity edge. |
| [`icm_edge_mild.png`](icm_edge_mild.png) | §13 | §13 ICM edge (3-player, ladder 50/30/20): per-init-seed ICM-reward minus chip-reward mean tournament prize over 6 reproducible seeds — ICM-reward UNDERPERFORMING the chip reward here (mean -146, 1/6 seeds ICM>chips). The earlier §13 small-positive estimate came from 3 noisier unseeded inits; at this scale per-seed variance (±300+) dominates, so there is no robust concavity edge. |
| [`blockB_action_grid.png`](blockB_action_grid.png) | §16 | B2 (§16): the finer 7-action grid does NOT beat the default 5-action grid (both strongly beat myopic) — extra sizings just spread the budget over more actions. Mean held-out chip diff vs myopic, per init seed. 5-grid stays the default. |
| [`blockB_bust_clip.png`](blockB_bust_clip.png) | §15 | B3 (§15): widening the multi-hand bust clip does NOT help — the tight 3.0 ('old') clip has the best mean held-out chip diff vs myopic (averaged over 6 init seeds); '4.6' and 'wide' (≈6.9) regress and destabilise some inits (high weight-init variance). |
| [`blockB_bust_rate.png`](blockB_bust_rate.png) | §15 | B3 (§15): bust rate per clip (avg over seeds). The tight 3.0 clip also has the lowest bust rate — in this heads-up winner-take-all format bust ≈ 1 − win rate, so there is no independent risk lever to recover by un-clipping the ruin signal. |
| [`blockB_selfplay.png`](blockB_selfplay.png) | §18 | B4 (§18): snapshot self-play vs the fixed-vs-myopic recipe, mean RL net chips vs each opponent (averaged over init seeds). Self-play trades the myopic bench for more tilt-robustness but is higher-variance — fixed stays the dependable recipe. |
| [`blockB_tilt_decouple.png`](blockB_tilt_decouple.png) | §17 | B5 (§17): tilt-bonus configurations, mean RL net chips vs each opponent. The PnL feature ALONE (pnl_nobonus) is best; the naive bonus collapses (footgun), and the decouple is safe but no net gain. The feature and the bonus are substitutes. |
| [`rollout_fe.png`](rollout_fe.png) | §14 | B1 (§14): warmed-belief rollout fold-equity, net chips of the rollout vs each opponent (60 paired seeds × 200 hands). Warming FIXES the cold-FE over-bluff disaster vs call-happy foes (myopic/tilt), but FE still isn't a uniform win (the nit punishes it), so fold-equity stays OFF by default. Blue = rollout wins chips. |
