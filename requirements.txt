treys==0.1.8
pytest==8.0.0
matplotlib==3.9.0
plotly==5.22.0
notebook==7.2.0
ipykernel==6.29.0
numpy==1.26.4
pandas==2.2.2
# Phase 2 — analytics dashboard
streamlit==1.36.0
pyarrow==16.1.0
# Phase A — fold equity (pure-Python betainc fallback exists if absent)
scipy==1.11.4
# Phase C — RL agent (OPTIONAL: rl_agent.py is torch-guarded; all other
# Phase C modules import and run without torch). Uncomment to train the DQN:
# torch>=2.0
